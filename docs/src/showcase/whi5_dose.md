# Whi5 dose sets the size

Start fires when Whi5 is diluted to a threshold, so the commitment size is set by the Whi5 *amount*:
`V* = W/θ`. Double the Whi5, double the size. Slide the dose:

```@raw html
<iframe src="../assets/widgets/whi5_dose.html" width="100%" height="500" frameborder="0" style="border:1px solid #ddd;border-radius:6px"></iframe>
```
