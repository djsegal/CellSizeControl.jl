name = "CellSizeControlDocs"

# Standalone environment for building the Documenter.jl site. Kept separate from
# the package's own Project.toml so the test environment stays unchanged. The
# package itself is loaded from the parent directory; see docs/make.jl.

[deps]
Documenter = "e30172f5-a6a5-5a46-863b-614d45cd2de4"
